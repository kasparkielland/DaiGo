<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="DaiGo.Views.ItemDetailPage"
             Title="{Binding Title}">

   
    <StackLayout Padding="40" BackgroundColor="{StaticResource agentThemeColor}">
        <Label Text="Description" Style="{StaticResource H2}" TextColor="{StaticResource darkTextColor}" VerticalOptions="Center" HorizontalOptions="End" />
        <Label Text="{Binding Item.Description}" d:Text="Description"  Style="{StaticResource H4}" BackgroundColor="{StaticResource lightTextColor}"/>
        <Label Text="Offer Price" Style="{StaticResource H2}" TextColor="{StaticResource darkTextColor}" VerticalOptions="Center" HorizontalOptions="End" />
        <Label Text="{Binding Item.Text}" d:Text="User's offer"    Style="{StaticResource H4}" BackgroundColor="{StaticResource lightTextColor}"/>
        <Button Text="Send Quote" Style="{StaticResource primaryButton}" VerticalOptions="EndAndExpand"/>
    </StackLayout>
</ContentPage>